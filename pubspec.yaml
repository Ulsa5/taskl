name: taskl
description: Gestor de Tareas.

publish_to: 'none' 
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    
    sdk: flutter
    
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^8.2.0
  url_launcher: ^6.0.3
  transparent_image: ^2.0.0
  fluttertoast: ^8.0.3
  

dev_dependencies:
  flutter_launcher_icons: ^0.8.0
  flutter_test:
    
    sdk: flutter
    
    
flutter:
  uses-material-design: true
  assets:
    - images/

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "images/icono.png"